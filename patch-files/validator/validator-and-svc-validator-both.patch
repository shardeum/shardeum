diff --git a/dev.Dockerfile b/dev.Dockerfile
index b1e0858..b798d3b 100644
--- a/dev.Dockerfile
+++ b/dev.Dockerfile
@@ -23,7 +23,9 @@ COPY . .
 RUN apt-get update && apt-get install -y \
     build-essential \
     curl
-RUN curl https://sh.rustup.rs -sSf | bash -s -- -y
+ARG RUST_VERSION=1.74.1
+RUN curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain $RUST_VERSION
+#RUN curl https://sh.rustup.rs -sSf | bash -s -- -y
 ENV PATH="/root/.cargo/bin:${PATH}"

 # Install node_modules
diff --git a/src/config/index.ts b/src/config/index.ts
index 1e0c8d7..276a519 100644
--- a/src/config/index.ts
+++ b/src/config/index.ts
@@ -146,7 +146,7 @@ config = merge(config, {
       amountToShrink: 5,
       maxDesiredMultiplier: 1.2,
       maxScaleReqs: 250, // todo: this will become a variable config but this should work for a 500 node demo
-      forceBogonFilteringOn: true,
+      forceBogonFilteringOn: process.env.forceBogonFilteringOn === undefined ? true : process.env.forceBogonFilteringOn === 'true',
       //these are new feature in 1.3.0, we can make them default:true in shardus-core later

       // 1.2.3 migration starts
@@ -306,11 +306,11 @@ config = merge(
   config,
   {
     server: {
-      mode: 'release', // todo: must set this to "release" for public networks or get security on endpoints. use "debug"
+      mode: process.env.mode || 'release', // todo: must set this to "release" for public networks or get security on endpoints. use "debug"
       // for easier debugging
       debug: {
-        startInFatalsLogMode: true, // true setting good for big aws test with nodes joining under stress.
-        startInErrorLogMode: false,
+        startInFatalsLogMode: process.env.startInFatalsLogMode === undefined ? true : process.env.startInFatalsLogMode === 'true', // true setting good for big aws test with nodes joining under stress.
+        startInErrorLogMode: process.env.startInErrorLogMode === undefined ? false : process.env.startInErrorLogMode === 'true',
         robustQueryDebug: false,
         fakeNetworkDelay: 0,
         disableSnapshots: true, // do not check in if set to false
diff --git a/src/shardeum/shardeumFlags.ts b/src/shardeum/shardeumFlags.ts
index 7de8dc2..1ebaa9c 100644
--- a/src/shardeum/shardeumFlags.ts
+++ b/src/shardeum/shardeumFlags.ts
@@ -126,7 +126,7 @@ export const ShardeumFlags: ShardeumFlags = {
   contractStoragePrefixBitLength: 3,
   contractCodeKeySilo: false,
   globalCodeBytes: false,
-  VerboseLogs: false,
+  VerboseLogs: process.env.VerboseLogs === undefined ? false : process.env.VerboseLogs === 'true',
   debugTraceLogs: false,
   Virtual0Address: true,
   GlobalNetworkAccount: true,
@@ -244,7 +244,7 @@ export const ShardeumFlags: ShardeumFlags = {
   // 1.5.7 migration
   supportEstimateGas: true,

-  startInServiceMode: false,
+  startInServiceMode: process.env.startInServiceMode === undefined ? false : process.env.startInServiceMode === 'true',
   allowedEndpointsInServiceMode: [
     'POST /contract/estimateGas',
     'POST /contract/call',