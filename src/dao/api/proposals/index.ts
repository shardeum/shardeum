import { all } from './all'
import { latest } from './latest'
import { count } from './count'
import { dev_all } from './dev_all'
import { dev_latest } from './dev_latest'
import { dev_count } from './dev_count'

export default {
  all,
  latest,
  count,
  dev_all,
  dev_latest,
  dev_count,
}
